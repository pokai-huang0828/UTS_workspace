# ⏸️ 暫停點 · 2026-08-09 01:15(撞 session 上限)· 4:10am 刷新 → 4:15 喚醒

> Kenny 已就寢並授權夜間全權推進(含 commit/push)。撞上限時的處置照他的指示:
> 讀出刷新時間 → 排刷新後喚醒 → 進度落檔 push → 停下來,不空轉重試。

---

## 一、這一輪(23:00–01:15)完成了什麼

| 里程碑 | 狀態 |
|---|---|
| **十頁內容 v2.1** | ✅ **定稿候選**,`04_十頁內容_v2.md`,12 單位齊全 |
| 配時(主迴圈程式實算) | ✅ **2,487 字 = 565.2 秒 = 9:25**,逐頁全在硬上限內,距 600 秒餘 35 秒 |
| 禁字 / 作廢數字 / 匿名化 | ✅ **全部 0 命中** |
| 色值統一 | ✅ `#E8833A` + `#1F4E79` |
| Codex 六條覆蓋層 | ✅ 全部落地(裁決 A′/J/K/L/M/N) |
| 十張圖 | ✅ 全部產出;**六張因可讀性重畫,已完成 6/7** |
| 出圖機具 | ✅ `scripts/make_figs_v4.py` + `build_deck_v4.py` + `figs/fig_v4_01..10.py` |

## 二、🔴 恢復後第一件事

### 1. 補跑 `fig_v4_07`(P7 倫理頁)—— **唯一沒做完的**

被 session 上限打斷。指令與其他六張相同,腳本在
`v4/scripts/figs/fig_v4_07.py`,依**裁決 O**:畫布鎖 `newfig(12.2, 5.7)`、
最小 fs=9、塞不下砍內容不放大畫布、帶狀純文字移出圖並在
`moved_to_slide` 逐字列出。P7 建議移出的是四張張力卡片的內文,
圖只留八原則對照表與紅線連線。

重跑指令(其餘六支走快取):
`Workflow({scriptPath:'…/a3-v4-figures-refit-wf_cada8295-101.js', resumeFromRunId:'wf_cada8295-101'})`
⚠️ **但 resumeFromRunId 只在同一 session 內有效** —— 若已換 session,改為只發一支 P7 的新 workflow。

### 2. 🔴 主迴圈必須自己做的獨立目視稽核(R-獨立,不可省)

六張重畫圖**只有產出者自驗**。主迴圈要:
- 用程式重算一次每張的實體吋數與 `fs9 → 有效 pt`(算法見 `12_出圖稽核與畫布裁決.md` §一)
- **逐張用 Read 打開親眼看**:文字重疊、裁切、一般筆電尺寸可讀性
- v3 有三張圖是講稿定稿時才發現文字重疊的,**這一關不能只看數字**

⚠️ **已知待裁決**:多支回報指出共用機具 `save()` 的 `bbox_inches='tight'` +
`pad_inches=0.1` 會在四邊各加 0.1 吋白邊,使輸出變成 12.4 x 5.9 而非 12.2 x 5.7。
→ 影響落地字級約 1.6%,可忽略;**但若要精確,就在 `save()` 加 `pad_inches=0`**。
沒有人動共用機具(其他支同時在跑),主迴圈裁決。

### 3. 組版 `build_deck_v4.py`

六張圖的 `moved_to_slide` 清單是**組版的施工圖**,內容非常詳細
(P2 那份就有 A/B/C/D/E/F 六大區、每格指定字級與色值)。
🔑 `fig_v4_05.py` 更好:移出的 14 塊已存成 module-level 的 `SLIDE_TEXT`,
組版程式可 `from figs.fig_v4_05 import SLIDE_TEXT` 直接取用,**不必從回報複製貼上**。
→ **建議請其他幾支也照這個做法把 SLIDE_TEXT 落進腳本**,避免漏字。

完整回報在 `C:\Users\kenny\AppData\Local\Temp\claude\D--KennyCode-UTS-workspace\
9e5e4b3d-f90a-41c5-a0e1-1ced3f89e1c9\tasks\w2lvhlb9z.output`。

### 4. 之後的順序

出圖收尾 → 組版(12 張投影片)→ **逐頁渲染稽核**(PowerPoint COM 導出逐頁 PNG,
v3 就是這樣才發現每頁標題出現兩次)→ 講稿定稿 + 配時程式驗算 →
答辯 defence bank + 5 分鐘計時演練 → 一分鐘試錄 + 鏡頭測試 → 彩排 ×2 → 逐頁錄製 → 8/17 交件。

---

## 三、還沒做、但已經備好的

- **12 題待 Kenny 回答** → `11_待Kenny回答_第二批.md`(Google Sheet 寫入失敗,見下)
- **參考文獻 15 條實地查證** —— 兩度發起都被中止,**還沒跑過**。
  直接對應評分老師連兩次點名的「引用要可溯源」,優先度高。
- **答辯 defence bank** —— Kenny 當初推翻 v3 就是因為沒把握答辯,而那 5 分鐘是現場計分。

## 四、Google Sheet 寫入失敗(誠實記錄)

目標 `1SpOguQTqGyzwZc-6eBSvBP06k85x0bHVvZWmNNUAuMY` gid 973024915,
資料在列 6–50(Q01–Q44),要 append 到列 51 起(Q45–Q56)。三種方法都失敗:
①逐格打字 —— 中文被吃掉(Sheets 要先進編輯模式,CDP 的 insertText 才寫得進去,
  ASCII 字元會開啟編輯模式所以只有 ASCII 進得去)
②`execCommand('copy')` —— 沒有使用者手勢被擋
③`navigator.clipboard.writeText` —— 權限 granted,但文件一取得焦點渲染器就凍結
**Kenny 的資料完好無損**:Q39–Q44 在列 45–50 原封不動,列 51 以下全空,
中途誤貼一次(剪貼簿裡是表格網址)已當場 Ctrl+Z 撤銷並逐格驗過。
→ **下次要寫,建議改用 Google Sheets API(`.mcp.json` 已設 google-workspace,
需重開 app 生效),不要再走瀏覽器。**

## 五、環境

這台 = **MSI Raider 18**。`gh` / `ffmpeg` / `ffprobe` / `yt-dlp` / `jupyter` **全缺**
(TODO 那張 2026-08-02 環境表是**另一台**的)。
🔴 `ffmpeg` 是 A3 影音驗收閘門的硬需求,**待 Kenny 點頭才裝**:
`winget install --id Gyan.FFmpeg -e --accept-package-agreements --accept-source-agreements`
✅ Codex 已更新 0.144.5 → **0.147.0**。
